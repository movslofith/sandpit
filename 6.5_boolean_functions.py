# def is_divisible(x, y):
#   if x % y == 0:
#        return True
#    else:
#        return False

def is_divisible(x, y): # this function performs the same role as the one defined above but more concisely
    return x % y == 0

is_divisible(3, 7)
